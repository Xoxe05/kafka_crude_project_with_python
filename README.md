# kafka_crude_project_with_python
***
- One Step forward, Two step back ahh stuff I did(myself, no AI, except for bugs related help. Also took total of idk, 2hrs a day max for like 8 days) that's been done by thousands if not millions(probably in java), but in python to learn about kafka basics.
- Kafka and elasticsearch setup in docker, with 3 kafka brokers configured sharing same internal ports but different external ports (simulating cluster), SASL security with plaintext config for kafka properties used, cause localhost baby (Not Putting Docker compose files here cause AI was used to build those compose.yaml).
- Does cover all the kafka basic topics, upto proper error handling and graceful exit upon full execution interruption(threaded). Rest, Figure this 🤮🤮 code yourself 
***
